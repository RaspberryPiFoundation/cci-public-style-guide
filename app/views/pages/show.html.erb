<% if !page.is_example_page? %>
  <h1 class="u-text--center"><%= page.title %></h1>

  <div class="c-content-panel">
    <%= render :partial => page.partial_path %>
  </div>
<% else %>
  <%= render :partial => page.partial_path %>
<% end %>
