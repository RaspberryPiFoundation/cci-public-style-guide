<h1>Editing Page - <%= @page.title %></h1>

<%= form_for(@page, :url => edit_admin_page_path(@page)) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Back', admin_path %>
