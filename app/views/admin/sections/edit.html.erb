<h1>Editing Section - <%= @section.title %></h1>

<%= form_for(@section, :url => admin_section_path(@section)) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<%= link_to 'Back', admin_index_path %>
